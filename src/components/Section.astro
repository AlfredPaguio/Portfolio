---
import { cn } from "@/lib/utils/cn";

interface Props {
  title?: string;
  id?: string;
  class?: string;
}
const { title, id, class: className } = Astro.props;
---

<section
  id={id}
  aria-labelledby={id ? `${id}-heading` : undefined}
  class={cn("relative py-8 lg:py-12", className)}
>
  {
    title && (
      <h2
        id={`${id}-heading`}
        class="text-lg font-bold uppercase tracking-widest text-neutral-500 mb-4"
      >
        {title}
      </h2>
    )
  }
  <slot />
</section>
